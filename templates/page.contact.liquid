{% layout 'theme' %}
{% section 'announcement-bar' %}
{% section 'header-about'%}
<div class="contained">
  <div class="page-width section-inner">
    <div class="section-header text-center limitcopywidth">
      <h1>{{ page.title }}</h1>
    </div>
    {{ page.content }}
  </div>
</div>
{% section 'contact-form' %}
